\documentclass{article}
\usepackage[utf8]{inputenc}
\usepackage{amsmath}
\usepackage{graphicx}
\usepackage{listings}
\usepackage{xcolor}
\usepackage{tikz}
% Load libraries for shapes, advanced arrowheads, and relative positioning
\usetikzlibrary{shapes.geometric, arrows.meta, positioning}
\lstset{
    basicstyle=\ttfamily,          % Uses the monospace code font
    keywordstyle=\color{blue},     % Colors keywords blue
    commentstyle=\color{green},    % Colors comments green
    breaklines=true                % Wraps long lines of code
}

\title{Custom SQL Journey Documentation}
\author{Awwab Wadekar}
\date{\today}

\begin{document}

\maketitle
% Introduction
\section{Introduction}

I was bored in my vacation, so I thought why not learn how SQL works under the hood with an attempt on a custom implementation?

This is torture. It was not fun.

Anyways the journey is a 5 phase mess:
\textit{
\begin{list}{Phase}\large
    \item{1.}{The OS background}
    \item{2.}{The Volcano Model}
    \item{3.}{The System Catalog}
    \item{4.}{The Parser Dilemma}
    \item{5.}{The Editor Interface}
\end{list}
}
\newpage
% Phase 1
\textbf{\section{Phase 1: The OS background}}


% Phase 1 : Day 1
\subsection{Day 1: Serialsation and Deserialisation}

\paragraph{
    Data is often stored in Raw Bytes in Memory. So when we fetch it we actually can't use something like struct.id or struct.name to access fields. \newline Instead we use an offset variable to keep track of what we have accessed.
}
\paragraph{
    Another thing is that often times when you use structs or classes, they use additional memory because of padding.
    To Explain Padding in simple terms, suppose you have a struct as below.
}
\begin{lstlisting}[language=C++]
    struct MyStruct{
        char name[30];
        int id;
    }
\end{lstlisting}
\paragraph{
    Now here the total memory is $30 * 1  + 4 = 34$ bytes, as one char is 1 byte and one int is 4 bytes.
    \newline But usually the OS assigns Memory in terms of Pages, which here are considered to be 4 bytes in size.
    So you have Page 1...Page 2...Page 3........Page 8...Page 9.
    Basically $9 * 4 = 36$ bytes.
    \newline Wait a minute, what ?
    I'm not a Mathematics Graduate but surely $36 > 34$.
    \newline So what actually happened here is padding, in the 8th Page, 2 bytes are used for the last 2 elements of the char array.
    \newline But we cant exactly put 2 bytes of an int in one page and 2 in another. And we for sure cannot have half a page.
    So the OS simply pads those 2 bytes up.
}
\paragraph{
    Well how do we fix this? The solution : Interpret everything as raw bytes and track offset of every field.
    Something along the lines of :
}  \mbox{} \\

\begin{lstlisting}[language=C++]
    vector<uint8_t> buffer;
    size_t offset = 0;
    MyStruct obj;
    obj.name = "Awwab";
    obj.id = 77;
    std::memcpy(buffer.data() + offset, &obj.name, sizeof(obj.name))
    offset += sizeof(obj.name);
    std::memcpy(buffer.data() + offset, &obj.id, sizeof(obj.id))
    offset += sizeof(obj.id);
\end{lstlisting}

\paragraph{
    Similarly, Deserialization does the same process, just this time the source and destination are swapped.
}  \mbox{} \\

\begin{lstlisting}[language=C++]
    vector<uint8_t> buffer;
    size_t offset = 0;
    MyStruct obj;
    std::memcpy(&obj.name, buffer.data() + offset, sizeof(obj.name))
    offset += sizeof(obj.name);
    std::memcpy(&obj.id, buffer.data() + offset, sizeof(obj.id))
    offset += sizeof(obj.id);
\end{lstlisting}

% Phase 1 : Day 2
\newpage
\subsection{Day 2: First Prototype of a Page}

\paragraph{
    A Page in reference to OS, is a kind of unit of storage.
    Usually when the computer wants to play around with data, it usually does so in the form of Pages.
    \newline Throughout this project we will see my page design get updated repeatedly.
    For now though, our Page is a somewhat simple class as given as below:
} \mbox{} \\

\begin{lstlisting}[language=C++]
    // struct for acting as a record
    struct [[gnu::packed]] UserRecord
    {
        uint32_t id;
        char username[32];
        uint32_t age;
    }; // [[gnu::packed]] Prevents compiler padding


    class Page
    {
    private:
        // Exactly 4096 bytes matching typical OS page size
        std::array<uint8_t, 4096> data_buffer;

        // Helper to calculate how large a single record is
        const size_t RECORD_SIZE = sizeof(uint32_t) + 32 + sizeof(uint32_t); // 40 bytes
        const size_t HEADER_SIZE = sizeof(uint32_t);                         // 4 bytes for storing record count

    public:
        Page()
        {
            data_buffer.fill(0); // Zero out the page initially
        }
        const uint8_t* ReadRawData() const { return data_buffer.data(); }
        uint8_t* WriteRawData() { return data_buffer.data(); }
        // Returns how many records are currently stored (read from the header)
        uint32_t GetRecordCount() const; // returns number of records currently in Page

        // Appends a record to the page if there is space
        bool AppendRecord(const UserRecord &record);

        // Retrieves a record by its slot index inside this page
        UserRecord GetRecord(uint32_t index) const;
    };
\end{lstlisting}

\paragraph{
    Although, we will change much functionality of this class in the future, for now it is a good starting point.
    We have a clear structure for how data is organized within a page, and the methods for reading and writing records are well-defined.
}
% Phase 1 : Day3
\newpage

\subsection{Day 3 : Connecting to the Disk}

\paragraph{
    As you might have learnt, data is stored in devices such as magnetic disksor hard drives.
    On a higher level, data is stored in files. We shall treat one file as basically disk.
    Much like how the read/write head moves accross sectors in a disk, our disk manager moves it's pointer
    across the file with the help of seekg and seekp.
    \newline A high level view is given below.
} \mbox{} \\

\begin{lstlisting}[language=C++]
    class DiskManager
    {
    private:
        std::fstream db_file;
        std::string filename;

    public:
        DiskManager(const std::string &db_filename) : filename(db_filename); // open file
        ~DiskManager(); // close file

        void WritePage(uint32_t page_id, const Page &page); // write to a page

        void ReadPage(uint32_t page_id, Page &out_page); // read a page
    };
\end{lstlisting}

\paragraph{
    Obviously you will notice that we don't have a truly complete logic yet.
    Mainly we aren't tracking the number of pages, nor is there a clear way defined for the bounds of the file.
    \newline This will be addressed in Day 4
}

% Phase 1 : Day 4
\newpage
\subsection{Day 4 : Refining the Disk Manager}

\paragraph{
    After looking at the shortcomings of our simple implementation of a disk manager yesterday, 
    we will extend our definition by 2 new helper functions.
} \mbox{} \\

\begin{lstlisting}[language=C++]
    // Returns the total number of 4KB blocks currently inside the file
    uint32_t GetTotalPages();
    // Grows the file by one blank page and returns its new Page ID
    uint32_t AllocatePage();
\end{lstlisting}

\paragraph{
    This definition will easily suffice for our needs for much of the journey.
    Much like a physical read/write head, it smoothly scans through our pages, reads the necessary data and writes at the correct locations.
    \newline Another benefit of this simplistic generic implementation is that we can often change the Page definition multiple times
    (as we will soon see in the later phases), we hardly have to change the logic of our disk manager.
    This is the power of Abstraction at play !
}

% Phase 1 : Day 5
\newpage
\subsection{Day 5 : First Query}

\paragraph{
    Although we obviously need to program a looootttt more code for solving any general query in the domain and rules clearly set by our language,
    but right now, we just want to show how we can use the current data structures for a specific query.
    \newline Right now our data structure stores records with 3 fields : username, id and age.
    We will perform a query equivalent to : Select username, age FROM ... WHERE age $\ge$ ... .
    \newline The logic is basically to scan every page in our disk, and then scans every record in a page.
    This type of sequential scan is a precursor to what is about to come :D.
} \mbox{} \\

\begin{lstlisting}[language=C++]
    void ScanAndPrintUsersOlderThan(DiskManager &disk, uint32_t target_age)
    {
        uint32_t total_pages = disk.GetTotalPages();
        // YOUR CODE HERE:
        // 1. Loop through every page_id from 0 to total_pages - 1
        for (uint32_t i = 0; i < total_pages; i++)
        {
            // 2. Inside the loop, declare a local stack Page instance
            Page local_page;
            // 3. Call disk.ReadPage(page_id, local_page)
            disk.ReadPage(i, local_page);
            // 4. Get the record count for that page
            uint32_t record_count = local_page.GetRecordCount();
            // 5. Loop through every record index in that page using local_page.GetRecord(i)
            for (uint32_t j = 0; j < record_count; j++)
            {
                UserRecord record = local_page.GetRecord(j);
                // 6. If record.age > target_age, print it to the console!
                if (record.age > target_age)
                    std::cout << "User " << record.username << " with Age : " << record.age << std::endl;
            }
        }
    }   
\end{lstlisting}

\paragraph{
    The above code is our function used to calculate the required result of the query.
    A more general approach that we will see later, will involve using a template or a sort of virtual class being used as a base,
    on top of which we can build complex expressions that can cover logical questions such as 'is age > x ?' and perhaps even mathematical evaluations.
}

% Phase 1 : Day 6
\newpage
\subsection{Day 6 : Buffer Pool Manager}

\paragraph{
    Now, imagine everytime we want some specific data , we have to go to the disk every single time.
    That would be highly resource consuming, and is usually not preferred when we are dealing with huge loads of data.
    So we need some sort of "buffer" to store frequently accessed data.
    \newline If our disk manager handles the structural "ROM" of our logic, then the buffer pool manager 
    handles the structural "RAM". The logic is basically :
    \newline \newline
    1. Check if Page exists in Buffer. \newline
    2. If yes, return it and push page to front. \newline
    3. Else first check buffer size. If it is at limit, evict the least recently used Page in the buffer. \newline
    4. After at least one free space is in the buffer, pull the page from disk and return it. \newline
    \newline
    A compact view of the class is given below.
} \mbox{} \\

\begin{lstlisting}[language=C++]
    class BufferPoolManager
    {
    private:
        static constexpr size_t POOL_SIZE = 3; // Our RAM can only hold 3 pages at once

        DiskManager &disk_manager;
        std::array<Page, POOL_SIZE> pool; // The physical slots (frames) in RAM
        size_t next_free_frame = 0;       // Tracks where to insert on a cache miss
        // Inside BufferPoolManager:
        // 1. The list stores page_ids. Front is MRU (Most Recently Used), Back is LRU.
        std::list<uint32_t> lru_list;

        // 2. The map stores the frame and the iterator (the "bookmark")
        struct PageMetadata
        {
            uint32_t frame_id;
            std::list<uint32_t>::iterator list_iterator;
            bool is_dirty;
        };
        std::unordered_map<uint32_t, PageMetadata> page_table;
        // Now maps page_id to {frame_id, iterator_to_list_node}
    public:
        uint32_t cache_hits = 0;
        uint32_t cache_misses = 0;

        BufferPoolManager(DiskManager &disk) : disk_manager(disk) {}

        void MarkDirty(uint32_t page_id); \\ mark a page as dirty to ensure data consistency.
        Page *FetchPage(uint32_t page_id); \\ Page ftching logic as given above
    };
\end{lstlisting}

% Phase 1 : Day 7
\newpage
\subsection{Day 7 : Chapter's close }
\paragraph{
    For a final test of our logic built so far, we will test writing to disk, using our buffer pool manager for optimal fetching,
    test our LRU logic, and ensure that changes written to a page on the buffer pool manager are written back to disk.
    \newline Thus our final "test" as such is given below.
} \mbox{} \\

\begin{lstlisting}[language=C++]
    std::string filename = "integration_test.db";
    std::remove(filename.c_str());

    // 1. Setup: Load a page
    {
        DiskManager disk(filename);
        uint32_t p0 = disk.AllocatePage();
        Page p0_data;
        p0_data.AppendRecord(UserRecord{1, "InitialData", 20});
        disk.WritePage(p0, p0_data);
    }

    // 2. Perform operations with Dirty Bit
    {
        DiskManager disk(filename);
        BufferPoolManager bpm(disk);

        // Fetch
        Page *p = bpm.FetchPage(0);

        // Modify the page in RAM
        UserRecord updated{1, "UpdatedData", 99};
        p->AppendRecord(updated); // Assume you added an Overwrite/Update method to Page
        // CRITICAL: Mark as dirty!
        bpm.MarkDirty(0);

        // Trigger eviction by filling the pool with other pages
        bpm.FetchPage(1);
        bpm.FetchPage(2);
        bpm.FetchPage(3); // Page 0 should now be evicted and written to disk
    }

    // 3. Verify Persistence
    {
        DiskManager disk(filename);
        Page p0_final;
        disk.ReadPage(0, p0_final);

        // If the dirty bit worked, this will be 99, not 20!
        assert(p0_final.GetRecord(1).age == 99);
        std::cout << "Dirty bit persistence test passed!" << std::endl;
    }

    std::remove(filename.c_str());    
\end{lstlisting}

\paragraph{
    With that our first phase is officially complete :D.
    Now our next phase will deal with making our current logic more abstract and generalised.
    We will also need to ensure appropriate logic for maintaining types for our custom SQL language.
    To do this we will be making a sort of "volcano like model" whose base is our current logic of fetching pages
    from the buffer / disk. The upper layers will have the responsibility to be able to perform the necessary complex operations
    on any data that is passed fro,m the base, according to a set of rules , that are designed to always be expandable.
}


\newpage
% Phase 2
\textbf{\section{Phase 2: The Volcano Model}}


% Phase 2 : Day 1
\subsection{Day 1: Schemas}

\paragraph{
    Ahh, one of the most central parts of SQL is the schema. It lets you define your data in a structured manner.
    To start off phase 2, we define the \textbf{Schema} class, the \textbf{Column} struct and the \textbf{TypeID} enum class.
    We begin with our types being only 2 : \textbf{INT32} and \textbf{VARCHAR}.
    A column is used to store the type, name length and offset within the schema as metadata.
    A schema is a collection of columns, that basically defines a table for us. Below is given the skeleton of our new code.
} \mbox{} \\

\begin{lstlisting}[language=C++]
    // Use an enum for performance-critical type checking
    enum class TypeId {
        INT32,
        VARCHAR
    };

    struct Column {
        std::string name;
        TypeId type;
        uint32_t length; // Size in bytes (e.g., 4 for INT32, N for VARCHAR)
        uint32_t offset; // Pre-calculated byte offset within a tuple, represents the size of all columns before it
    };

    class Schema {
    private:
        std::vector<Column> columns;
        uint32_t tuple_size = 0; // Total size of a single row in bytes

    public:
        // Explicitly define a schema by passing a list of columns
        Schema(const std::vector<Column>& input_columns) : columns(input_columns);
        

        ~Schema() = default;

        const std::vector<Column>& GetColumns(); // returns columns as a read only value
        
        uint32_t GetTupleSize(); // returns the size of the tuple

        uint32_t GetColumnCount(); // returns total number of columns

        const Column& GetColumn(uint32_t col_idx); // returns a specific column
    };
\end{lstlisting}

% Phase 2 : Day 2
\newpage
\subsection{Day 2 : Terrific Tuples}

\paragraph{
    When we will be fetching data from our Pages , now we cannot pass them as raw bytes for the higher level executors.
    Instead we actually need a sort of generalised structure that works for any specific type.
    Enter the \textbf{tuple} class : a model that allows us to fetch data from any Page, check the appropriate type and return the raw data in that type.
    We also have a struct called as \textbf{RID}, which we use to uniquely identify the location of our Page and the slot of our record.
    Here, the slot value will be more relevant in the next phase when we update some of our code in phase 1.
    A basic idea of today's work is as follows : 
} \mbox{} \\

\begin{lstlisting} [language=C++]
    // Uniquely identifies a physical record on disk
    struct RID
    {
        uint32_t page_id;
        uint32_t slot_num;

        RID() : page_id(0), slot_num(0) {}
        RID(uint32_t page_id, uint32_t slot_num) : page_id(page_id), slot_num(slot_num) {}
    };

    class Tuple
    {
    private:
        const uint8_t *data_ptr_; // Zero-copy pointer to the buffer pool page
        uint32_t size_;           // Size of the tuple in bytes
        RID rid_;                 // Physical location of this tuple

    public:
        Tuple() : data_ptr_(nullptr), size_(0) {}

        // Initialize the tuple with a pointer to the raw page data
        Tuple(const uint8_t *data_ptr, uint32_t size, RID rid)
            : data_ptr_(data_ptr), size_(size), rid_(rid) {}

        ~Tuple() = default;

        RID GetRID(); // returns rid
        const uint8_t *GetData(); // returns ptr to start of Page

        // Extracts an integer field using the Schema's pre-calculated offset
        int32_t GetInt32(const Schema &schema, uint32_t col_idx);
        // Extracts a string field using the Schema's pre-calculated offset
        std::string GetVarchar(const Schema &schema, uint32_t col_idx);
    };
\end{lstlisting}

% Phase 2 : Day 3
\newpage
\subsection{Day 3 : The Heart of the Volcano}

\paragraph{
    Today we will be building the virtual classes that power our most core commands.
    Any kind of SQL program follows the following pipeline: \newline
    FROM - \textgreater ON - \textgreater JOIN - \textgreater WHERE - \textgreater GROUP BY - \textgreater HAVING - \textgreater SELECT - \textgreater DISTINCT - \textgreater ORDER BY - \textgreater LIMIT / OFFSET
    \newline For our project, we will mostly be dealing with :
    \newline FROM - \textgreater WHERE - \textgreater SELECT .
    \newline However, the baseline is the same, which is what we have been building towards :
    Fetch data from pages, strictly maintain schema and build expressions on top of abstract classes.
    Some of our virtual baseline is given as below :
} \mbox{} \\

\begin{lstlisting}[language=C++]
    // executor_context.hpp
    class ExecutorContext {
    private:
        BufferPoolManager &bpm_;

    public:
        explicit ExecutorContext(BufferPoolManager &bpm) : bpm_(bpm) {}
        ~ExecutorContext() = default;

        // Disallow copying for memory safety
        ExecutorContext(const ExecutorContext&) = delete;
        ExecutorContext& operator=(const ExecutorContext&) = delete;

        BufferPoolManager* GetBufferPoolManager() { return &bpm_; }
    };

    // abstract_executor.hpp
    class AbstractExecutor {
    protected:
        ExecutorContext *exec_ctx_;

    public:
        explicit AbstractExecutor(ExecutorContext *exec_ctx) : exec_ctx_(exec_ctx) {}
        virtual ~AbstractExecutor() = default;

        /**
        * Initializes the executor. 
        * Inside this method, internal states (like pointers or counters) should be reset.
        */
        virtual void Init() = 0;

        /**
        * Yields the next tuple from this operator.
        * @param tuple The next tuple produced by this operator (output parameter)
        * @param rid The physical record identifier of the returned tuple (output parameter)
        * @return true if a tuple was successfully produced, false if there are no more tuples (EOF)
        */
        virtual bool Next(Tuple *tuple, RID *rid) = 0;

        /** @return The schema of the tuples produced by this executor */
        virtual const Schema& GetOutputSchema() const = 0;
    };

    // abstract_expression.hpp
    class AbstractExpression
    {
    public:
        virtual ~AbstractExpression() = default;

        // Evaluates the expression against a specific physical tuple
        virtual Value Evaluate(const Tuple *tuple, const Schema &schema) const = 0;
    };


\end{lstlisting}

\paragraph{
    One thing you will notice is that our abstractions use something called as a \textbf{Value}.
    This is a new class defined for ensuring the general nature of our virtual core. A view of the class is given below :
} \mbox{} \\

\begin{lstlisting}[language=C++]
    class Value {
    private:
        TypeId type_;
        union {
            int32_t int32_val;
        };
        std::string varchar_val; // Handled separately due to non-trivial constructor

    public:
        Value() : type_(TypeId::INT32), int32_val(0) {}
        Value(int32_t val) : type_(TypeId::INT32), int32_val(val) {}
        Value(std::string val) : type_(TypeId::VARCHAR), varchar_val(val) {}

        TypeId GetType(); // returns types of the value
        int32_t AsInt32(); // returns integer value
        std::string AsVarchar(); // returns varchar value
    };
    
\end{lstlisting}

\paragraph{
    With the core setup, we can create a wide range of expressions to evaluate conditions for our data or simply fetch it in some manner.
    We will see this in the coming days. 
    For now we simply note our cycle as search :
    Init - \textgreater Next - \textgreater Next - \textgreater Next ..... till condition evaluates to false. - \textgreater Init
} 

% Phase 2 : Day 4
\newpage
\subsection{Day 4 : Sequential Scanner}

\paragraph{
    The first executor we will build is a rather simple one. It simply starts at some page, scans every record in that page,
    goes to next page... until it runs out of pages. Every iteration of \textbf{Next} takes it to the next record.
    For simplcity , this executor scans page ids in increasing order, ie 0..1..2.. and so on.
    Our Page class has also been upgraded so that it can work for any generic tuple. Both the classes are given below briefly :
} \mbox{} \\

\begin{lstlisting}[language=C++]
// Upgraded Page class
class Page {
private:
    std::array<uint8_t, 4096> data_buffer;
    static constexpr size_t HEADER_SIZE = sizeof(uint32_t); // 4 bytes for record count

public:
    Page() {
        data_buffer.fill(0); 
    }
    // Standard read/write methods
    const uint8_t* ReadRawData();
    uint8_t* WriteRawData();

    uint32_t GetRecordCount(); // Gets number of records

    // Generic append: Accepts a raw pointer to any serialized tuple data and its size
    bool AppendRecord(const uint8_t* record_data, uint32_t record_size);

    // Zero-Copy Accessor: Returns a direct pointer to the starting byte of a slot
    const uint8_t* GetRecordPtr(uint32_t index, uint32_t record_size);
};

// SeqScanExecutor
class SeqScanExecutor : public AbstractExecutor {
private:
    Schema schema_;
    uint32_t total_pages_;
    
    // Iteration state
    uint32_t current_page_id_;
    uint32_t current_slot_num_;
    uint32_t pages_scanned_;
    uint32_t start_page_id_;


public:
    SeqScanExecutor(ExecutorContext *exec_ctx, const Schema &schema, uint32_t total_pages, uint32_t start_page_id = 0)
        : AbstractExecutor(exec_ctx), schema_(schema), total_pages_(total_pages),
          current_page_id_(start_page_id), current_slot_num_(0), start_page_id_(start_page_id), pages_scanned_(0) {}

    void Init() override; // Set's current_page_id to start_page_id

    bool Next(Tuple *tuple, RID *rid) override; // Advances to next record in page or first record of next page
    // returns false if no more pages to scan

    const Schema& GetOutputSchema() const override;
};
\end{lstlisting}
% Phase 2 : Day 5
\newpage
\subsection{Day 5 : Abstract Expressions}

\paragraph{
    Our \textbf{WHERE} Clause will be a collection of boolean checks, like age \textgreater \  18, marks \textless \  100, etc.
    To evaluate these clauses, we will use a generic expression class.\newline
    \textbf{ColumnValueExpression} and \textbf{ConstantValueExpression} are the Operand types.
    While we will build expressions similar to \textbf{ComparisonExpression}, where we take 2 operands and perform the greater than operation.
    A view of these classes is given below.
} \mbox{} \\

\begin{lstlisting}[language=C++]
    // Virtual Base Class
    class AbstractExpression
    {
    public:
        virtual ~AbstractExpression() = default;

        // Evaluates the expression against a specific physical tuple
        virtual Value Evaluate(const Tuple *tuple, const Schema &schema) const = 0;
    };

    // Represents a literal constant value inside a query (e.g., the number 21)
    class ConstantValueExpression : public AbstractExpression
    {
    private:
        Value val_;

    public:
        explicit ConstantValueExpression(Value val) : val_(val) {}
        // Returns a constant value
        Value Evaluate(const Tuple *tuple, const Schema &schema) const override
    };

    // Represents a variable reference to a column inside a tuple (e.g., table.age)
    class ColumnValueExpression : public AbstractExpression
    {
    private:
        uint32_t col_idx_;

    public:
        explicit ColumnValueExpression(uint32_t col_idx) : col_idx_(col_idx) {}

        // Fetches a column using the data from tuple and the schema for offset
        Value Evaluate(const Tuple *tuple, const Schema &schema) const override
    };

    // Represents a binary comparison operation (For today, we will explicitly implement GT '>')
    class ComparisonExpression : public AbstractExpression
    {
    private:
        std::unique_ptr<AbstractExpression> left_;
        std::unique_ptr<AbstractExpression> right_;

    public:
        ComparisonExpression(std::unique_ptr<AbstractExpression> left, std::unique_ptr<AbstractExpression> right)
            : left_(std::move(left)), right_(std::move(right)) {}

        // Evaluates both children, asserts same type values and compares their values, returns true if lhs > rhs
        Value Evaluate(const Tuple *tuple, const Schema &schema) const override
        
    };
    
\end{lstlisting}

% Phase 2 : Day 6
\newpage
\subsection{Day 6 : Filtering Fun}

\paragraph{
    Continuing from Day 6, we are building towards our \textbf{WHERE} clause.
    In the past days we have built both Abstract Expressions and Sequential Scanner. Now we will link both to a single unit.
    \newline The filter executor calls a child abstract executor, (mostly our sequential scanner) then checks if the current row satisfies the expression given.
    This expression is the predicate. 
} \mbox{} \\

\paragraph{
    A rough view of this new executor class is given below :
} \mbox{} \\

\begin{lstlisting}[language=C++]
    class FilterExecutor : public AbstractExecutor
    {
    private:
        std::unique_ptr<AbstractExecutor> child_executor_;
        std::unique_ptr<AbstractExpression> predicate_;

    public:
        FilterExecutor(ExecutorContext *exec_ctx,
                    std::unique_ptr<AbstractExecutor> child_executor,
                    std::unique_ptr<AbstractExpression> predicate)
            : AbstractExecutor(exec_ctx),
            child_executor_(std::move(child_executor)),
            predicate_(std::move(predicate)) {}

        // Calls child_executor->Init()
        void Init() override
        

        // Collects tuple from child executor, evaluates it against predicate, returns if true
        // Else it encounters EOF or tries next row
        bool Next(Tuple *tuple, RID *rid) override
        
        // A filter doesn't mutate rows; its output schema is identical to its child's schema
        const Schema &GetOutputSchema() const override
    };
\end{lstlisting}

% Phase 2 : Day 7
\newpage
\subsection{Day 7 : Pinning it with Projection }
\paragraph{
    Our final Executor for this phase completes the \textit{SELECT ... FROM ... WHERE... } pipeline.
    It collects the Valid rows and selects the columns asked.
    \newline For this it uses:
    \newline 1. A child executor, often a filter executor.
    \newline 2. A schema describing the new columns
    \newline 3. A list of column expressions denoting the selected columns from the input
    \newline To store these values, we use a temporary buffer
    It's outline is given below :
} \mbox{} \\

\begin{lstlisting}[language=C++]
class ProjectionExecutor : public AbstractExecutor
{
private:
    std::unique_ptr<AbstractExecutor> child_executor_;
    Schema output_schema_;
    std::vector<std::unique_ptr<AbstractExpression>> expressions_;

    // Local workspace buffer to hold the newly materialized tuple data
    std::vector<uint8_t> tuple_buffer_;

public:
    // Set the variables and
    // Resize our local buffer to match the exact byte size of our target output layout
    ProjectionExecutor(ExecutorContext *exec_ctx,
                       std::unique_ptr<AbstractExecutor> child_executor,
                       Schema output_schema,
                       std::vector<std::unique_ptr<AbstractExpression>> expressions)
        : AbstractExecutor(exec_ctx),
          child_executor_(std::move(child_executor)),
          output_schema_(output_schema),
          expressions_(std::move(expressions))
    {
        tuple_buffer_.resize(output_schema_.GetTupleSize());
    }
    // Calls child_executor->Init()
    void Init() override

    // Pulls the next row from child to return target columns or returns EOF
    bool Next(Tuple *tuple, RID *rid) override

    // returns target schema for result
    const Schema &GetOutputSchema() const override
};
\end{lstlisting}

% Phase 2 : Day 8
\newpage
\subsection{Day 8 : End to End Showcase}
\paragraph{
    Whew, that took too long. Anyways We are gonna be wrapping up this phase by creating a custom schema, selecting target columns,
    specifying the filter condition and finally verifying our results. A brief overview of the testing pipeline is: 
} \mbox{} \\
\begin{lstlisting}[language=C++]
    void TestEndToEndPipeline()
    {
        std::string filename = "end_to_end_db.bin";
        std::remove(filename.c_str());

        DiskManager disk(filename);
        BufferPoolManager bpm(disk);
        ExecutorContext exec_ctx(bpm);

        // 1. Core Source Layout: { id (0), username (1), age (2) }
        std::vector<Column> src_cols = {
            {"id", TypeId::INT32, 4, 0},
            {"username", TypeId::VARCHAR, 32, 0},
            {"age", TypeId::INT32, 4, 0}};
        Schema src_schema(src_cols);
        uint32_t tuple_size = src_schema.GetTupleSize();

        // 2. Populate Disk Frame: Create 5 users spread over 3 pages
        uint32_t p0_id = disk.AllocatePage();
        uint32_t p1_id = disk.AllocatePage();
        uint32_t p2_id = disk.AllocatePage();

        // Page 0: Alice (ID: 1, Age: 25), Bob (ID: 2, Age: 30)
        Page *p0 = bpm.FetchPage(p0_id);
        std::array<uint8_t, 40> alice{0};
        int32_t id1 = 1;
        int32_t age1 = 25;
        std::memcpy(alice.data() + 0, &id1, 4);
        std::memcpy(alice.data() + 4, "alice", 5);
        std::memcpy(alice.data() + 36, &age1, 4);
        p0->AppendRecord(alice.data(), tuple_size);

        std::array<uint8_t, 40> bob{0};
        int32_t id2 = 2;
        int32_t age2 = 30;
        std::memcpy(bob.data() + 0, &id2, 4);
        std::memcpy(bob.data() + 4, "bob", 3);
        std::memcpy(bob.data() + 36, &age2, 4);
        p0->AppendRecord(bob.data(), tuple_size);
        bpm.MarkDirty(p0_id);

        // Page 1: Charlie (ID: 3, Age: 35)
        Page *p1 = bpm.FetchPage(p1_id);
        std::array<uint8_t, 40> charlie{0};
        int32_t id3 = 3;
        int32_t age3 = 35;
        std::memcpy(charlie.data() + 0, &id3, 4);
        std::memcpy(charlie.data() + 4, "charlie", 7);
        std::memcpy(charlie.data() + 36, &age3, 4);
        p1->AppendRecord(charlie.data(), tuple_size);
        bpm.MarkDirty(p1_id);

        Page *p2 = bpm.FetchPage(p2_id);
        std::array<uint8_t, 40> donovan{0};
        int32_t id4 = 4;
        int32_t age4 = 40;
        std::memcpy(donovan.data() + 0, &id4, 4);
        std::memcpy(donovan.data() + 4, "donovan", 7);
        std::memcpy(donovan.data() + 36, &age4, 4);
        p2->AppendRecord(donovan.data(), tuple_size);

        std::array<uint8_t, 40> eve{0};
        int32_t id5 = 2;
        int32_t age5 = 23;
        std::memcpy(eve.data() + 0, &id5, 4);
        std::memcpy(eve.data() + 4, "eve", 3);
        std::memcpy(eve.data() + 36, &age5, 4);
        p2->AppendRecord(eve.data(), tuple_size);
        bpm.MarkDirty(p2_id);

        // 3. TARGET PROJECTION SCHEMA: { age (0), id (1) }
        std::vector<Column> dest_cols = {
            {"age", TypeId::INT32, 4, 0},
            {"id", TypeId::INT32, 4, 0}};
        Schema dest_schema(dest_cols);

        // ==========================================
        // WIRE UP THE VOLCANO PIPELINE TREE
        // ==========================================

        // Step A: Instantiate the base sequential scanner unique pointer
        auto scan_node = std::make_unique<SeqScanExecutor>(&exec_ctx, src_schema, disk.GetTotalPages());

        // Step B: Formulate the filter predicate condition (age > 26)
        auto col_expr = std::make_unique<ColumnValueExpression>(2); // age index
        auto const_expr = std::make_unique<ConstantValueExpression>(Value(26));
        auto filter_predicate = std::make_unique<ComparisonExpression>(std::move(col_expr), std::move(const_expr));

        // Step C: Wrap the scan node inside the Filter Executor unique pointer
        auto filter_node = std::make_unique<FilterExecutor>(&exec_ctx, std::move(scan_node), std::move(filter_predicate));

        // Step D: Map out the projection remapping parameters
        std::vector<std::unique_ptr<AbstractExpression>> proj_exprs;
        proj_exprs.push_back(std::make_unique<ColumnValueExpression>(2)); // Map source age to dest col 0
        proj_exprs.push_back(std::make_unique<ColumnValueExpression>(0)); // Map source id to dest col 1

        // Step E: Wrap the filter node inside the final root Projection Executor node
        // YOUR CODE HERE: Initialize the projection plan coordinator node
        ProjectionExecutor plan_root(&exec_ctx, std::move(filter_node), dest_schema, std::move(proj_exprs));

        // Initialize root execution pipeline
        plan_root.Init();

        Tuple t;
        RID r;

        // First execution iteration: Should filter Alice (25), skip to Bob (30), and drop "username"
        assert(plan_root.Next(&t, &r) == true);
        assert(t.GetInt32(dest_schema, 0) == 30); // Bob's Age mapped to index 0
        assert(t.GetInt32(dest_schema, 1) == 2);  // Bob's ID mapped to index 1

        // Second execution iteration: Should smoothly cross physical disk blocks to yield Charlie (35)
        assert(plan_root.Next(&t, &r) == true);
        assert(t.GetInt32(dest_schema, 0) == 35); // Charlie's Age mapped to index 0
        assert(t.GetInt32(dest_schema, 1) == 3);  // Charlie's ID mapped to index 1

        // Third execution iteration: Should smoothly cross physical disk blocks to yield Donovan (40)
        assert(plan_root.Next(&t, &r) == true);
        assert(t.GetInt32(dest_schema, 0) == 40); // Donovan's Age mapped to index 0
        assert(t.GetInt32(dest_schema, 1) == 4);  // Donovan's ID mapped to index 1
        // Final call: Plan execution complete (EOF)
        assert(plan_root.Next(&t, &r) == false);

        std::cout << "DAY 8 END-TO-END PIPELINE WORKS PERFECTLY! PHASE 2 COMPLETE!" << std::endl;
        std::remove(filename.c_str());
    }
\end{lstlisting}

\begin{figure}[htbp]
\centering
\begin{tikzpicture}[
    % Define element styles
    startstop/.style={rectangle, rounded corners, minimum width=2.5cm, minimum height=1cm, text centered, draw=black, fill=red!15},
    process/.style={rectangle, minimum width=3cm, minimum height=1cm, text centered, draw=black, fill=orange!15},
    decision/.style={diamond, aspect=2, minimum width=3cm, minimum height=1cm, text centered, draw=black, fill=green!15},
    arrow/.style={thick, ->, >=Stealth}
]

    % Place nodes sequentially downwards
    \node (start)     [startstop] {Start};
    \node (eof)       [decision, below=1cm of start] {is EOF?};
    \node (seq_scan)  [process, below=1.2cm of eof] {Sequential Scan};
    \node (filt_scan) [process, below=1.2cm of seq_scan] {Filter Scan};
    \node (pred_chk)  [decision, below=1.2cm of filt_scan] {Predicate Check?};
    
    % Place the outlier nodes
    \node (proj_exec)   [process, below=1.2cm of pred_chk] {Project target columns};
    \node (ret_row)     [process, below=1.2cm of proj_exec] {Return Row};
    \node (end_node)    [startstop, right=2cm of eof] {End}; % Branch EOF to end

    % Draw main vertical flow connections
    \draw [arrow] (start) -- (eof);
    \draw [arrow] (eof) -- node[anchor=west] {No} (seq_scan);
    \draw [arrow] (seq_scan) -- (filt_scan);
    \draw [arrow] (filt_scan) -- (pred_chk);
    \draw [arrow] (pred_chk) -- node[anchor=west] {Yes} (proj_exec);
    \draw [arrow] (proj_exec) -- (ret_row);
    
    % EOF Yes branch to End
    \draw [arrow] (eof) -- node[anchor=south] {Yes} (end_node);
    
    % Main processing loop (Predicate Check 'Yes' logic, continues vertically)
    \draw [arrow] (ret_row.south) -- ++(0,-0.7cm) -- node[anchor=west, pos=0.2] {Next Row} ++(-2.5cm,0) |- (start.west);
    
    % Predicate Check 'No' branch loops back to start
    % This loop indicates that if the predicate fails, the process immediately fetches the next row.
    \draw [arrow] (pred_chk.west) -- node[anchor=south, pos=0.3] {No} ++(-1.2cm,0) |- (start.south west);

\end{tikzpicture}
\caption{Row scanning and predicate evaluation loop.}
\label{fig:row_scan_flowchart_vert}
\end{figure}

\clearpage
% Phase 3
\textbf{\section{Phase 3 : The System Catalog}}

% Phase 3 : Day 1
\subsection{Day 1 : Table Metadata}

\paragraph{
    Now as we know, each database, has some sort of metadata that helps to make precise queries ahead.
    That is exactly what this phase aims to tackle. Throughout this phase we will work with a single class
    \textit{catalog}.
    \newline For today we will define two functions : \newline
    1. createTable : tableName, schema \newline
    2. GetTableSchema : Name\newline
    createTable takes in the name of a table, and the schema of the new table.
    1st it checks if the table exists, if it does return false and stop.
    Else create the table metadata. We have a fixed schema structure for table metadata in this form :
} \mbox{} \\

\begin{lstlisting}[language=C++]
    const Schema tab_schema({
        {const_cast<char *>("name"), TypeId::VARCHAR, 64, 0, 1},
        {const_cast<char *>("first_page_id"), TypeId::INT32, 4, 0, 0},
        {const_cast<char *>("rows"), TypeId::INT32, 4, 0, 0},
        {const_cast<char *>("schema_page_id"), TypeId::INT32, 4, 0, 0},
        {const_cast<char *>("column_count"), TypeId::INT32, 4, 0, 0},
    });

    const Schema col_schema({
        {const_cast<char *>("name"), TypeId::VARCHAR, 32, 0, 1},
        {const_cast<char *>("length"), TypeId::INT32, 4, 0, 0},
        {const_cast<char *>("type"), TypeId::INT32, 4, 0, 0},
        {const_cast<char *>("is_in_candidate_key"), TypeId::INT32, 4, 0, 0},
    });
\end{lstlisting}

\paragraph{
    GetTableSchema uses our \textit{Filter Executor} from previous phase to search for whether or not the table exists.
    It then uses the predefined \textit{tab\_schema} given above to fetch the table basic data. It uses the \textit{schema\_page\_id} field
    to get the schema from there. Our structure is : \newline
    1. The whole Database can be considered as a Forest of Linked Lists. \newline
    2. The head is Page 0 which has the table Metadata. \newline
    3. From the rows of this table, we can get 2 head for each table. \newline
    4. One head is the starting page defining the columns of the table. \newline
    5. The other head is the starting page of the actual rows of that table. \newline
    6. Each list's tail points to \textit{0xFFFFFFFF} as their next page, indicating the end of list. \newline
    Tomorrow we'll try for insertion of rows.
}

% Phase 3 : Day 2
\newpage
\subsection{Day 2 : Insert Intelligence}

\paragraph{
    Today we add our new function of \textit{InsertRow}. We also used some helpers like \newline
    1. \textit{getTuple} \ : \ Validates a list of values against a given schema and returns them in a tuple ready for insertion. \newline
    2. \textit{updateRowCount}\ : \ Updates the row count in the db metadata and writes to disk, called after write operations that change number of rows.\newline
    3. \textit{fetchRowCount}\ : \ Fetches the row count from the table metadata. \newline
    4. \textit{fetchRowCount\_RAW}\ : \ Calculates row count by physically reading the disk. \newline
    Using these, our insert function inserts a list of given rows. Here atomicity is followed. If even one row is not matching with schema, we reject all.
    We used an insertion executor for this process. It is similar in nature to the SeqScanExecutor, in how it scans pages. An optional parameter helps count number of inserts, As duplicate candidate key rows are ignored.
} \mbox{} \\

\begin{lstlisting}[language=C++]
class InsertionExecutor : public AbstractExecutor
{
private:
    Schema schema_;

    // Iteration state
    uint32_t current_page_id_;
    uint32_t previous_page_id_;
    uint32_t pages_scanned_;
    uint32_t start_page_id_;

public:
    InsertionExecutor(ExecutorContext *exec_ctx, const Schema &schema, uint32_t start_page_id = 0, uint32_t previous_page_id = 0xFFFFFFFF)
        : AbstractExecutor(exec_ctx), schema_(schema), current_page_id_(start_page_id), start_page_id_(start_page_id), pages_scanned_(0), previous_page_id_(previous_page_id) {}

    // Reset execution cursors to point to the very first slot on the first page
    void Init() override;

    // Insert if you can, for now returns false only if the size of the record exceeds the maximum possible slot size in a page
    bool Next(Tuple *tuple, RID *rid) override;

    // schema of insertion
    const Schema &GetOutputSchema() const override;
};
\end{lstlisting}

\paragraph{
    Except this time insead of using \textit{GetTuplePtr} to read, it uses \textit{InsertTuple} to write, using our page class.
    We have to note to mark the pages dirty as we are writing using our \textbf{BufferPoolManager}.    
    Also we borrowed a Query operation from the future to check for duplicate rows. Oopss. Off to Day 3 \!
}

% Phase 3 : Day 3
\newpage
\subsection{Day 3 : Quick Queries}

\paragraph{
    Reaching the halfway point, today we will be implementing the \textit{Query} function.
    This function needs the table name and columns to select from that table as part of main arguments.
    Additionally, an abstract expression is accepted for filtering and also a list of names which are to be used in output.
    By default, all rows are returned and column names are unaltered.
    We use our full pipeline from phase 2 here, i.e. \newline
    \textbf{SeqScan Executor - \textgreater Filter Executor - \textgreater Projection Executor} \newline
    To help with formatting, the results are returned as a tuple of Bool (whether query was valid),
    Schema(of the selected columns renamed) and vector of Tuple (result rows).
    \newline \newline
    We also have some new helper functions: \newline \newline \newline
    1. \textit{print\_table} : Used to print tables. It uses a given Schema and a list of rows to print the table
    in a format similar to postgresql/mysql in the terminal.
    \newline \newline
    2. \textit{GetSchemaCols} : It returns the column metadata of a table, given the name. It returns this meta data as a vector of tuples.
    Using that and our static \textit{col\_schema}, we can easily print it with the \textit{print\_table} function.
    \newline \newline
    3. \textit{GetDBMeta} : It takes no argument and returns the basic info of tables. The schema of this metadata is the predefined 
    \textit{tab\_schema}.
    \newline \newline
    4. \textit{makeCNF}: It is currently used for checking if the row to be inserted exists or not by making a CNF expression.
}
% Phase 3 : Day 4
\newpage
\subsection{Day 4 : Updates Underway}
\paragraph{
    Today we will work on \textit{UpdateRow} function. This functton updates specified columns in the given table according to some condition.
    For simplicity of implementation, we will not allow any changes to columns that are part of the candidate key.
    To streamline the process we have made a new executor for updates as well, as given below.
} \mbox{} \\
\begin{lstlisting}[language=C++]
class UpdateExecutor : public AbstractExecutor
{
private:
    // child is mostly a filter executor
    std::unique_ptr<AbstractExecutor> child_executor_;
    std::map<std::string, Value> col_value_map_;

public:
    UpdateExecutor(ExecutorContext *exec_ctx, std::unique_ptr<AbstractExecutor> child_executor, std::map<std::string, Value> col_value_map) : AbstractExecutor(exec_ctx), child_executor_(std::move(child_executor)), col_value_map_(col_value_map) {}

    // Initialize child
    void Init() override
    
    // Update rows given by child or hit eof
    bool Next(Tuple *tuple, RID *rid) override

    // A update doesn't mutate rows; its output schema is identical to its child's schema
    const Schema &GetOutputSchema() const override
};
\end{lstlisting}

\paragraph{
    This executor is also used in certain other places such as updating metadata as well like rowcount.
    Much like Insert, it is first ensured that the update is valid, before being actually implemented.
    Also an extra pointer variable/address can be supplied to fetch the count of affected rows.
}

% Phase 3 : Day 5
\newpage
\subsection{Day 5 : Delete Dance}
\paragraph{
    Finally we close out the last functions based on Deleting rows and entire Table.
    For Deleting a row, we introduce a new executor : \textit{DeleteExecutor} , which functions quite similarly to update.
    Here after the child executor returns the rows according to the conditions, we use our previously defined delete function from our page class.
    An overview of this new executor is given below.
} \mbox{} \\

\begin{lstlisting}[language=C++]
class DeleteExecutor : public AbstractExecutor
{
private:
    // child, mostly filter executor
    std::unique_ptr<AbstractExecutor> child_executor_;
public:
    DeleteExecutor(ExecutorContext *exec_ctx, std::unique_ptr<AbstractExecutor> child_executor) : AbstractExecutor(exec_ctx), child_executor_(std::move(child_executor)){}

    // Initialise child
    void Init() override

    // Delete the row given by child
    bool Next(Tuple *tuple, RID *rid) override

    // A delete doesn't mutate rows; its output schema is identical to its child's schema
    const Schema &GetOutputSchema() const override
};
\end{lstlisting}

\paragraph{
    On the other hand for deleting table we needed a few adjustments to our Page class.
    Keeping in line with our tombstone style delete, we add an extra flag in the header which indicates whether it is alive or not.
    To delete a page, we overwrite the bytes of everything else to default state and change this flag.
    This way we dont have to fragment memory as such. And these "dead" pages can be reused in our updated \textit{AllocatePage} function from
    our disk manager. A small snippet of important points below :
} \mbox{} \\

\begin{lstlisting}[language=C++]
    //new functions in Page class
    // sets alive page off (tombstone)
    void DeletePage()

    // check whether page is alive or not
    bool isAlive()

    //caller from disk manager to delete a page
    bool DeletePage(uint32_t page_id)

    //buffer pool manager caller
    // remove from Buffer Pool and call delete on the page and its successors
    uint32_t DeletePageHelper(uint32_t page_id)

    // function in catalog
    bool DeleteTable(std::string tableName)
\end{lstlisting}

% Phase 3 : Day 6
\newpage
\subsection{Day 6 : I'm Tired Boss}
\paragraph{
    Finally we reached the end of Phase 3. This was the brain and heart of our project, as you can see it above.
    This will now seamlessly be connected to the parser and frontend that will allow to execute the commands from the terminal !
    To recap, in this phase , we :\newline
    1. Made the catalog class with support to handle creation and deletion of tables, insertion, updation and deletion of rows, querying the table and some helper functions. \newline
    2. Made 3 new executors for insert, update and delete. \newline
    3. Updated Page, DiskManager, and BufferPoolManager classes. \newline
    4. Conducted extensive testing to the best of my ability to ensure no bugs or vulnerabilities are exploited. \newline 
}

\begin{figure}[htbp]
\centering
\begin{tikzpicture}[
    % Define element styles
    startstop/.style={rectangle, rounded corners, minimum width=2.5cm, minimum height=1cm, text centered, draw=black, fill=red!15},
    process/.style={rectangle, minimum width=3cm, minimum height=1cm, text centered, draw=black, fill=orange!15},
    decision/.style={diamond, aspect=2, minimum width=3cm, minimum height=1cm, text centered, draw=black, fill=green!15},
    arrow/.style={thick, ->, >=Stealth}
]

    % Place nodes sequentially downwards
    \node (start)     [startstop] {Start};
    \node (init)       [process, below=0.6cm of start] {Catalog creates .db file if it doesn't exist};
    \node (fetch_meta)  [process, below=0.6cm of init] {Fetch relevant information from metadata from Page 0};
    \node (validate) [decision, below=0.6cm of fetch_meta] {Validate the input operation};
    \node (execute)  [process, below=0.6cm of validate] {Use relevant Executors for operation};
    \node (commit)  [process, below=0.6cm of execute] {Save to DB};
    % Place the outlier nodes
    \node (end_node)    [startstop, right=2cm of init] {End}; % Branch to end

    % Draw main vertical flow connections
    \draw [arrow] (start) -- (init);
    \draw [arrow] (init) -- (fetch_meta);
    \draw [arrow] (fetch_meta) -- (validate);
    \draw [arrow] (validate) -- node[anchor=west] {Yes} (execute);
    \draw [arrow] (execute) -- (commit);
    
    % branch to End
    \draw [arrow] (init) -- (end_node);
    
    % Main processing loop
    \draw [arrow] (commit.south) -- ++(0,-0.7cm) -- node[anchor=west, pos=0.3] {Next Operation} ++(-4.5cm,0) |- (init.west);
    
    % Validation 'No' branch loops back to init
    % This loop indicates that if the validation fails, the operation is not peformed at all.
    \draw [arrow] (validate.west) -- node[anchor=south, pos=0.3] {No} ++(-2.5cm,0) |- (init.south west);

\end{tikzpicture}
\caption{Catalog Operation Loop}
\label{fig:catalog_flowchart}
\end{figure}

\clearpage
% Phase 4 : yet to come
\textbf{\section{Phase 4: Automata PTSD}}


\end{document}
